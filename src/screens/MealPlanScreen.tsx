import React from 'react';
import { ScrollView } from 'react-native';

export default class MealPlanScreen extends React.Component {
    render() {
        return (
            <ScrollView style={{ flex: 1, backgroundColor: '#ff0000' }}>
            </ScrollView>
        );
    }
}
